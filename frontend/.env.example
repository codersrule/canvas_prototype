# Backend URL (required at **build** time for Vite). Leave unset = mock login, no API.
# Local: VITE_API_URL=http://localhost:3001
# Render: set in Static Site Environment to your Web Service URL, then redeploy:
# VITE_API_URL=https://canvas-prototype-9voc.onrender.com
